import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'coding.prompt',
  appName: 'coding-prompt',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
